<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= data.title %>
    </title>
    <!-- Liên kết tới CSS của Bootstrap 5 từ Bootstrap CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="../../../public/admin/style.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container">
                <a class="navbar-brand" href="#">Trang Admin</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ml-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="#">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/listNews">Quản lí tin</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/createNews">Thêm tin</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/listUsers">Quản lí tài khoản</a>
                        </li>
                        <% if (data.req.session.admin) { %>
                            <li class="nav-item ml-auto">
                                <a class="nav-link" href="/admin/logout">Đăng xuất</a>
                            </li>
                        <% }else{%>
                            <li class="nav-item">
                                <a class="nav-link" href="/admin/login">Đăng nhập</a>
                            </li>
                        <% } %>
                        <!-- Thêm các mục menu khác vào đây -->
                    </ul>
                </div>
            </div>
        </nav>
    </header>

